import pandas #pandas already installed via powershell so it works
import hashlib #grabbing/including builtin module
# can check builtin module by writing 'builtin module in python' in GOOGLE
print("Hi") #if not installed will show error in printing hi
#import tensorflow 
# if not installed tensorflow will show error can install by using pip install tensorflow either in terminal or in powershell
#it is an external module
